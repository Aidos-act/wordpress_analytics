apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: stg
  name: rails-ing
  annotations:
    kubernetes.io/ingress.global-static-ip-name: "web-static-ip"
spec:
  backend:
    serviceName: web
    servicePort: 80