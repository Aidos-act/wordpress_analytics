apiVersion: v1
kind: Service
metadata:
  namespace: stg
  name: mysql
spec:
  selector:
    app: mysql
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 3306
      targetPort: 3306
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: stg
  name: mysql
spec:
  selector:
    matchLabels:
      app: mysql
  strategy:
    type: Recreate
  replicas: 1    
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:5.7
        env:
        - name: MYSQL_USERNAME
          value: 'root'
        - name: MYSQL_ROOT_PASSWORD
          value: 'root'
        - name: MYSQL_PASSWORD
          value: 'root'
        ports:
        - containerPort: 3306
          name: mysql
        volumeMounts:
        - name: mysql-pv
          mountPath: /var/lib/mysql           
      volumes:
      - name: mysql-pv
        persistentVolumeClaim:
          claimName: mysql-pvc          